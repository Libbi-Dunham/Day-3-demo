const benny = {
    name: 'Benny'
    age: 6,
    breeds: ['chihauhau', 'terrier', 'pekingese']

};

console.log(benny);
console.log(benny.name)
console.log(benny.breeds[1]);

// bracket 1 is to get terrier

// can out arrays inside objects

// can have an object inside an object/ can chain it with a dot